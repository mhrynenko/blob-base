log:
  disable_sentry: true

db:
  url: postgres://max:pass@localhost:5462/blobDB?sslmode=disable

listener:
  addr: :8000

cop:
  disabled: true
  endpoint: "http://..."
  upstream: "http://..."
  service_name: blob-base
  service_port: 80
